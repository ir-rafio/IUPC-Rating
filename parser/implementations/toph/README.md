# TophParser

`TophParser` reads Toph standings and detects the supported HTML layout from
each standings row. It supports both the older and current Toph layouts.

Select it with `"parser": "TOPH"` in `parser/input/contests.json`.
