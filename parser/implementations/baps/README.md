# BapsParser

`BapsParser` reads BAPS standings tables and extracts rank, team name, raw
institution name, solved count, penalty, and first-solve count.

Select it with `"parser": "BAPS"` in `parser/input/contests.json`.
